import { version } from './../../package.json';

export const SWAGGER_CONFIG = {
  title: 'Splitlyback',
  description: 'Docs for Splitly app purpose ',
  version: version,
  path: 'api/docs',
  bearerAuthName: 'Authorization',
};
